{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/scripts/plugin-install-deps.sh\"" },
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/slopweaver\" init >/dev/null 2>&1 || true" }
        ]
      }
    ]
  }
}
